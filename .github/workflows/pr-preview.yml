name: PR Preview

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Remove CNAME for preview
        run: rm -f CNAME

      - name: Update canonical URL for preview
        run: |
          # Remove canonical URL to prevent any SEO-related issues
          find . -name "*.html" -exec sed -i 's|<link rel="canonical" href="https://www.vollkommenbraut.de[^"]*">||g' {} \;

      - name: Deploy to GitHub Pages Preview
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: ./
          preview-branch: gh-pages
          umbrella-dir: pr-preview
          action: auto

